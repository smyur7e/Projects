var f = prompt("Enter your temperature in Fahrenheit");

// Convert the value entered into an integer
// f = parseInt(f);

// Type coersion from string to number
c = (f - 32) * 5 / 9;

alert(f
	+ " degrees fahrenheit is "
	+ c + " degrees celcius.");